/**
 * Excellence section lazy-loaded components.
 * Default export uses getter-based lazy loading for React.lazy compatibility.
 * Consultancy and EHS have been archived (moved to ../archive/).
 */
const ExcellencePages = {};

export default ExcellencePages;

// Named re-exports removed - components archived to ../archive/
// export { default as Consultancy } from './Consultancy';
// export { default as EHS } from './EHS';